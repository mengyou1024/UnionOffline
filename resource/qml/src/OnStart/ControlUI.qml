import QtQuick
import QtQuick.Controls
import QtQuick.Layouts
import QtQuick.Dialogs
import "../../components"

Rectangle {
    LoggingCategory {
        id: category
        name: "OnStart.ctrl"
    }
    color: "transparent"
    border.width: 1
    border.color: "#d8d8d8"
}
